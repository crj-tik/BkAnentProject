server:
  port: 9004

spring:
  application:
    name: listing-master-service
  config:
    import: optional:nacos:${spring.application.name}.yaml?group=DEFAULT_GROUP&refreshEnabled=true
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
      config:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        file-extension: yaml

dubbo:
  application:
    name: listing-master-service
  protocol:
    name: dubbo
    port: 20884
  registry:
    address: nacos://${NACOS_SERVER:127.0.0.1:8848}
